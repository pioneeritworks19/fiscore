part of '../../main.dart';

/// Placeholder boundary for the explicit local persistence layer described in
/// the sync strategy. Firestore offline persistence is useful, but audits,
/// evidence, and conflict-aware violation updates will need a durable local
/// database and operation queue as the app matures.
class LocalDatabase {
  const LocalDatabase();
}
